# 2026-08-01 APPGuide 引导插件改动总结

## 一、核心功能：客户端缓存完整 Flow JSON + 离线续接

### 背景
系统原来只存 `flowId` + `globalStepNumber` 到 `chrome.storage.local`，跨页续接完全依赖服务端 URL 匹配。每次续接都需要调 API。

### 目标
- 缓存完整流程 JSON 到本地，续接时 0 次网络请求
- 按 `pageIndex` 精确定位，不跳到流程中其他页
- URL 不匹配时弹确认框让用户决定

### 改动文件

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `plugin/background.js` | 新增 | `fetch-flow-by-id` 消息代理（~20行），调 `/rest?method=appguide.flows.byid` |
| `plugin/content.js` | 新增 8 个函数（~200行） | URL 匹配引擎、页面解析、fetchFlowById、normalizeFlowData、makeDraggable、showUrlMismatchDialog |
| `plugin/content.js` | 修改 8 处现有函数 | persistFlowState、startGuideFromResolved、advanceStep、autoResumeFlowOnLoad、enableGuide、handleGuideApiResult 等 |
| `plugin/content.css` | 新增 4 个类 | `.guide-mismatch-actions`、`.guide-mismatch-btn`、`.guide-mismatch-btn-primary` 及 hover 状态 |
| `plugin/popup.js` | 修改存储键值 | `apiBaseUrl` → `appguide_apiBaseUrl` |

---

## 二、修复：跳步问题

### 问题
原先 `resolvePageInFlowClient` 对流程中**所有页面**做 URL 匹配，用户若在流程第 5 页的 URL 上会直接跳到第 5 页。

### 修复
改为 `state.pageIndex` 精确定位目标页，只比对**这一页**的 URL：

```
state.pageIndex → cachedFlow.pages[pageIndex].url → urlsMatchClient(该url, 当前url)
  ├─ 匹配 → resolvePageByIndex → 渲染
  └─ 不匹配 → showUrlMismatchDialog → 用户决定
```

---

## 三、存储键值命名空间隔离

### 问题
引导插件和审核插件共用一个 `chrome.storage.local`，审核插件会读取引导插件的记录。

### 改动

| 键值 | 旧名 | 新名 |
|---|---|---|
| `chrome.storage` | `"guideFlowState"` | `"appguide_flowState"` |
| `chrome.storage` | `"apiBaseUrl"` | `"appguide_apiBaseUrl"` |
| `localStorage` | `"__appguideDebug"` | `"appguide_debug"` |

涉及 `content.js`（1处）、`background.js`（4处）、`popup.js`（3处）。

---

## 四、UI 改进

### 4.1 按钮样式移到 CSS
原先确认框按钮使用内联 `style="..."`，改为 CSS 类 `.guide-mismatch-btn` / `.guide-mismatch-btn-primary`。同时去掉 `class="guide-candidate-item"`（该类有 `!important` 覆盖内联样式）。

### 4.2 中止引导清除状态
"中止引导流程" 按钮从只关闭弹窗改为 `clearFlowState()` + toast 提示。

### 4.3 浮动窗口可拖动
新增 `makeDraggable()` 函数，按住标题栏可拖动 `showUrlMismatchDialog` 和 `renderCandidateChooser` 两个浮动窗口。按钮点击不受影响。

---

## 五、数据流架构

```
首次 Alt+G → API(/guide) 获取第0页
  └─ startGuideFromResolved → fetchFlowById 异步缓存全量 JSON
       └─ normalizeFlowData: steps 字符串→对象归一化
       └─ 存入 chrome.storage.local.cachedFlow

完成第0页 → persistFlowState(n+1, pageIndex+1)

导航到第1页 → autoResumeFlowOnLoad
  └─ 读 cachedFlow → 按 pageIndex 定位 → URL 匹配
       ├─ 匹配 → resolvePageByIndex → 0次网络请求 ✅
       └─ 不匹配 → showUrlMismatchDialog → 用户选择继续/放弃
```

---

## 六、关键函数说明

| 函数 | 作用 |
|---|---|
| `urlsMatchClient` | 客户端 URL 匹配（移植自服务端，支持动态 ID 识别） |
| `resolvePageByIndex` | 不做 URL 匹配，直接按索引返回 ResolvedPage |
| `resolvePageInFlowClient` | 做全页 URL 匹配（仅保留，实际续接已不再使用） |
| `fetchFlowById` | 调 byid 接口获取完整 flow |
| `normalizeFlowData` | 兼容 steps 的 3 种格式（字符串/数组/对象） |
| `showUrlMismatchDialog` | URL 不匹配确认框（含拖动） |
| `makeDraggable` | 通用拖动函数 |
